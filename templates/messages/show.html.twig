{% extends 'base.html.twig' %}

{% block body %}
    {% if message and message.deletedAt is null %}
        {{ message.body }}
    {% else %}
        It seems like this message has been deleted.
    {% endif %}
{% endblock %}
